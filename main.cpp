#include <iostream>    // Includes the default library, used for output and input data 

using namespace std;        // No need to use (std::) if using namespace std

int main() {           // Function main  
   
    int number = 5;
    cout << "Result: " << number;     // for printing to the console, and creates a new line after ending current 
    
    return 0;           // indication of program running successfully 
}

